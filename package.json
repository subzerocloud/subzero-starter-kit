{
  "name": "subzero-starter-kit-tests",
  "dependencies": {},
  "devDependencies": {
    "dotenv": "4.0.0",
    "jsonwebtoken": "^8.3.0",
    "mocha": "^8.2.1",
    "should": "^11.2.0",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "test_graphql": "mocha  --no-timeouts ./tests/graphql/",
    "test_rest": "mocha --no-timeouts ./tests/rest/",
    "test_db": "node tests/bin/test_db.js",
    "test": "$npm_execpath run test_db && $npm_execpath run test_rest && $npm_execpath run test_graphql"
  },
  "author": "ruslan.talpa@subzero.cloud",
  "license": "ISC"
}
